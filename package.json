{
	"name": "cts-data",
	"type": "module",
	"scripts": {
		"build": "tsc",
		"fetch-month": "tsc && node build/fetcher/fetch-last-month.js",
		"fetch-all": "tsc && node build/fetcher/fetch-historical.js",
		"build-website": "tsc && node build/builder/website.js"
	},
	"dependencies": {
		"puppeteer": "^21.3.6",
		"ts-node": "^10.9.1"
	},
	"devDependencies": {
		"typescript": "^5.2.2"
	}
}
